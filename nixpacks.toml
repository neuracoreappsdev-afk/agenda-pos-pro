[phases.setup]
nixPkgs = ["php74", "php74Extensions.pdo", "php74Extensions.pdo_sqlite", "php74Extensions.mbstring", "php74Extensions.gd", "php74Extensions.bcmath"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
